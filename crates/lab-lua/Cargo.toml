[package]
name = "lab-lua"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
lab-core = { path = "../lab-core" }
mlua = { version = "=0.11.6", default-features = false, features = ["lua54", "vendored"] }

[lints]
workspace = true
